% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-metafor.R
\name{as_bef_data}
\alias{as_bef_data}
\alias{as_bef_data.default}
\alias{as_bef_data.list}
\alias{as_bef_data.escalc}
\title{Convert input data to a bayesEfron data object}
\usage{
as_bef_data(x, ...)

\method{as_bef_data}{default}(x, ...)

\method{as_bef_data}{list}(x, ...)

\method{as_bef_data}{escalc}(x, ...)
}
\arguments{
\item{x}{Object to convert. v0.1 supports \code{bef_data}, \code{list} with
\code{theta_hat} and \code{sigma}, and \code{\link[metafor:escalc]{metafor::escalc()}} objects.}

\item{...}{Reserved for future expansion; must be empty in v0.1.}
}
\value{
A validated \code{bef_data} object with a \code{source} attribute set
to one of \code{"list"}, \code{"metafor::escalc"}, or unchanged for already
converted inputs.
}
\description{
Adapt the user's effect-size data into the standalone \code{bef_data}
class that \code{\link[=bayes_efron_fit]{bayes_efron_fit()}} consumes. Three input shapes are
supported, all returning a validated \code{bef_data} object that
carries \code{theta_hat}, \code{sigma}, optional site labels, and a \code{source}
attribute recording the input shape that produced it.
}
\details{
Supported input shapes:\tabular{lll}{
   Input class \tab Required fields \tab Source label \cr
   \code{bef_data} \tab (already converted; revalidated and returned) \tab unchanged \cr
   named \code{list} \tab \code{theta_hat}, \code{sigma}; optional \code{names} \tab \code{"list"} \cr
   \code{escalc} from \code{\link[metafor:escalc]{metafor::escalc()}} \tab \code{yi}, \code{vi} (variance); optional row labels \tab \code{"metafor::escalc"} \cr
}


For the \code{escalc} path, \code{theta_hat} is taken from \code{yi} and \code{sigma}
from \code{sqrt(vi)} after a strict-positivity check on \code{vi}. Row names
on the \code{escalc} object are propagated as site labels when present
and non-default.

For the \code{list} path, an explicit \code{names} element overrides any
names attribute on \code{theta_hat}. Site labels are dropped when they
are missing, empty, or contain \code{NA}.

Unsupported input classes raise a typed \code{bef_error} via
\code{\link[=as_bef_data.default]{as_bef_data.default()}} with the offending class name in the
message.
}
\examples{
# Plain list input.
dat_list <- as_bef_data(list(
  theta_hat = c(-0.21, 0.04, 0.19, 0.38, 0.61),
  sigma     = c( 0.18, 0.15, 0.22, 0.19, 0.24)
))
dat_list

# Optional site labels (must satisfy the minimum-length-5 constraint).
dat_named <- as_bef_data(list(
  theta_hat = c(
    site_1 = -0.21, site_2 = 0.04, site_3 = 0.19,
    site_4 =  0.38, site_5 = 0.61
  ),
  sigma = c(0.18, 0.15, 0.22, 0.19, 0.24)
))

# metafor::escalc() bridge.
if (requireNamespace("metafor", quietly = TRUE)) {
  esc <- metafor::escalc(
    measure = "MD",
    m1i  = c(0.10, 0.40, 0.55, 0.30, 0.20),
    sd1i = c(0.30, 0.30, 0.35, 0.28, 0.32),
    n1i  = c( 60,   55,   62,   58,   65),
    m2i  = c(0.05, 0.10, 0.15, 0.08, 0.12),
    sd2i = c(0.32, 0.34, 0.36, 0.30, 0.33),
    n2i  = c( 60,   55,   62,   58,   65)
  )
  dat_esc <- as_bef_data(esc)
  dat_esc
}

}
\seealso{
\itemize{
\item \code{\link[=bayes_efron_fit]{bayes_efron_fit()}}, which calls \code{as_bef_data()} internally on
its \code{theta_hat} / \code{sigma} arguments.
\item \code{\link[metafor:escalc]{metafor::escalc()}} for computing effect sizes and sampling
variances from study-level summary data.
}
}
