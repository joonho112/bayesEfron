% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{diagnose}
\alias{diagnose}
\alias{diagnose.default}
\alias{diagnose.bef_fit}
\title{Extract sampler diagnostics from a bayesEfron fit}
\usage{
diagnose(fit, ...)

\method{diagnose}{default}(fit, ...)

\method{diagnose}{bef_fit}(fit, ...)
}
\arguments{
\item{fit}{A \code{bef_fit} object returned by \code{\link[=bayes_efron_fit]{bayes_efron_fit()}}.}

\item{...}{Reserved for future expansion; must be empty in v0.1.}
}
\value{
A validated \code{bef_diagnostic} object with the schema
tabulated in \strong{Details}.
}
\description{
Return a structured \code{bef_diagnostic} object that bundles the
sampler-health and model-quality summaries already computed by
\code{\link[=bayes_efron_fit]{bayes_efron_fit()}}. The function does not re-process draws or
run new computations; it surfaces information that the fit
pipeline stored as attributes of \code{fit$metadata}, behind the
supported access path.

Use \code{diagnose()} together with \code{summary(fit)} to triage a fit:
\code{summary()} gives the user-facing posterior summaries; \code{diagnose()}
gives the structured sampler-health view that pairs with
\code{plot(fit, type = "diagnostic")}.
}
\section{The \code{bef_diagnostic} schema}{
A \code{bef_diagnostic} object carries:\tabular{lll}{
   Field \tab Type \tab Meaning \cr
   \code{rhat} \tab numeric vector \tab Per-parameter R-hat (Gelman–Rubin) statistic. Values close to 1 (typically \verb{< 1.01}) indicate convergence. \cr
   \code{ess_bulk} \tab numeric vector \tab Per-parameter bulk effective sample size; large values support reliable posterior means. \cr
   \code{ess_tail} \tab numeric vector \tab Per-parameter tail ESS; large values support reliable tail-quantile / interval estimates. \cr
   \code{divergences} \tab numeric vector or NA \tab Number of divergent transitions per chain. \cr
   \code{max_treedepth} \tab numeric vector or NA \tab Number of max-treedepth saturations per chain. \cr
   \code{effective_params_summary} \tab named list \tab Posterior summary of effective parameters. \cr
   \code{model_family} \tab character \tab \code{"RE"} for v0.1. \cr
   \code{stan_file_sha256} \tab character \tab SHA-256 of the locked Stan source used for the fit. \cr
   \code{runtime_seconds} \tab numeric \tab Sampler wall-clock for the fit. \cr
   \code{diagnostic_skipped} \tab character vector \tab Diagnostics intentionally not computed (rare). \cr
   \code{sampler_diagnostics_failed} \tab character vector \tab Diagnostics requested but failed at extract time (rare). \cr
}


The returned object is validated internally before return.
}

\section{Reading the result}{
Call \code{summary()} on a \code{bef_diagnostic} for the most-extreme value
per field (max R-hat, min ESS, total divergences, total
max-treedepth saturations); call \code{print()} for a one-screen
textual view. The \code{plot(fit, type = "diagnostic")} view consumes
the same attribute payload through this generic.
}

\examples{
# Load the cached five-site smoke fit shipped with the package.
fit <- readRDS(system.file(
  "examples", "cached_fit_re_smoke.rds",
  package = "bayesEfron"
))

diag <- diagnose(fit)
print(diag)
summary(diag)

}
\seealso{
\itemize{
\item \code{\link[=bayes_efron_fit]{bayes_efron_fit()}} for the upstream pipeline that populates
these diagnostics.
\item \link[=bayesEfron-methods]{summary.bef_diagnostic()} for the
extreme-value summary.
\item \code{\link[=plot.bef_fit_re]{plot.bef_fit_re()}} for the graphical companion view.
\item The methodological vignette M6 ("Verification and
calibration") for what each diagnostic means in the context of
the package's verification ledger.
}
}
