% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan-cache.R
\name{bayes_efron_clear_cache}
\alias{bayes_efron_clear_cache}
\title{Clear bayesEfron compilation cache artifacts}
\usage{
bayes_efron_clear_cache(
  scope = c("lock_only", "session", "compiled_models", "all")
)
}
\arguments{
\item{scope}{Character scalar. One of \code{"lock_only"} (default),
\code{"session"}, \code{"compiled_models"}, or \code{"all"}.}
}
\value{
Invisibly, a named integer vector with elements
\code{lock_files}, \code{session_keys}, \code{disk_models}, and
\code{disk_sidecars}. Each element counts the number of artifacts
removed; the contract is fixed even when the scope leaves a
given counter at zero.
}
\description{
Remove selected in-session and on-disk cache artifacts used by the
CmdStan compilation cache. Intended for cache maintenance,
stale-lock recovery after an interrupted compile, and forced
rebuilds during development.

Choose the smallest scope that resolves the issue at hand. The
\code{"lock_only"} scope is the safest and is appropriate for clearing
a stale lock left behind by a killed process. \code{"all"} is the
largest scope and will force every subsequent fit in this session
(and on this machine, until the cache is repopulated) to recompile
from scratch.
}
\section{Scopes}{
\tabular{lll}{
   Scope \tab Removes \tab Use when \cr
   \code{"lock_only"} \tab Stale lock file \tab Resuming after an interrupted compile that left a lock behind. \cr
   \code{"session"} \tab The in-session cache only \tab Forcing the current session to re-attach to disk-cached binaries. \cr
   \code{"compiled_models"} \tab Cache-entry binaries, sidecar JSON, and companion Stan-source copies under the current cache format directory; preserves the cache directory and lock file \tab Forcing a recompile while keeping the cache root intact. \cr
   \code{"all"} \tab The entire cache root, plus the in-session cache \tab Resetting the cache to a clean slate. \cr
}


This function does \strong{not} acquire the cache lock. Avoid calling
the disk-clearing scopes while another R process is compiling a
bayesEfron model in the same cache root.

The cache root location is controlled by the environment variable
\code{BAYESEFRON_CACHE_ROOT} (with a sensible per-user default if
unset).
}

\examples{
# Non-destructive: clear a stale lock if one is present.
bayes_efron_clear_cache("lock_only")

\dontrun{
# Destructive: drop the current session's cache entries.
bayes_efron_clear_cache("session")

# Most destructive: reset the entire cache root.
bayes_efron_clear_cache("all")
}

}
\seealso{
\itemize{
\item \code{\link[=bayes_efron_compile]{bayes_efron_compile()}} for repopulating the cache after a
clear.
\item \code{\link[=bayes_efron_fit]{bayes_efron_fit()}} for the user-facing fit pipeline that
consumes the cache.
}
}
